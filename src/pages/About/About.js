import styled from "styled-components";

const Wrapper = styled.div`
    width: 100%;
`;

export default function About() {
    return (
        <Wrapper id="about">
            About section
        </Wrapper>
    )
}