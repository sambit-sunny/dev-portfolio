import styled from "styled-components";

const Wrapper = styled.div`
    width: 100%;
`;

export default function Contact() {
    return (
        <Wrapper id="contact">
            Contact section
        </Wrapper>
    )
}