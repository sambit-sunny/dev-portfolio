import styled from "styled-components";

const Wrapper = styled.div`
    width: 100%;
`;

export default function Projects() {
    return (
        <Wrapper id="projects">
            Projects section
        </Wrapper>
    )
}